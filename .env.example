# === WinCVEx Environment Variables ===

# PostgreSQL
POSTGRES_USER=wincvex
POSTGRES_PASSWORD=change_me
POSTGRES_DB=wincvex

# API (FastAPI)
API_PORT=8000
API_HOST=0.0.0.0
JWT_SECRET=supersecretkey
JWT_EXPIRE_MINUTES=60
JWT_REFRESH_MINUTES=10080  # one week

# Frontend
NEXT_PUBLIC_API_BASE=http://localhost:8000

# Redis
REDIS_HOST=redis
REDIS_PORT=6379

# Grafana, Loki, Prometheus defaults
GRAFANA_USER=admin
GRAFANA_PASSWORD=admin

# Safety
ENVIRONMENT=local
